{
  "name": "novra-oauth-bootstrap",
  "version": "1.0.0",
  "private": true,
  "description": "Temporary server-side WordPress.com OAuth bootstrap. CommonJS on purpose - see tsconfig.json. Delete after the access token is stored.",
  "engines": {
    "node": ">=20"
  },
  "type": "commonjs"
}
